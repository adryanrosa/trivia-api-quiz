const INITIAL_STATE = ({
  category: '',
  difficulty: '',
});

const settings = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default: return state;
  }
};

export default settings;
